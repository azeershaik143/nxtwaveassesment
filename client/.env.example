# Local dev: leave these COMMENTED OUT so requests use Vite’s proxy (/api + /socket.io → :4000).
# Only set when you need the browser to talk to the API directly (then match CLIENT_ORIGIN on the server).
# VITE_API_URL=http://127.0.0.1:4000
# VITE_SOCKET_URL=http://127.0.0.1:4000
